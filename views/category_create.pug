extends layout 

block title
  = "Create new category"

block content 
  form(method="post")
    label(for="name") Name
    input(type="text", id="name", name="name" value=category && category.name)
    label(for="description") Description
    input(type="text", id="description", name="description" value=category && category.description)
    button Submit
  if errors
    div.errors
      ul
        each error of errors
          li= error.msg


