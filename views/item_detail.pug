extends layout

block title
  ="Item: " + item.name
block content
  div.flex.flex-col.items-center.m-6.gap-6
    div.flex.flex-col.items-center.gap-3.bg-gray-400.p-6.rounded-lg.shadow.w-96
      h2.text-3xl= item.name
      p= item.category.name
      p.self-center ₹ 
        span.font-medium.text-2xl #{item.price}
      p.self-center
        span.font-medium.text-2xl #{item.number_in_stock} 
        .
          in stock
      p #{item.description}
    .flex.gap-6
      a(href=`${item.url}/update`).rounded.px-3.py-1.shadow-lg.bg-gray-400 Update
      a(href=`${item.url}/delete`).rounded.px-3.py-1.shadow-lg.bg-gray-400 Delete
